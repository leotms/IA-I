COMPILE WITH:

  make <name_of_the_problem>.stateCount N=<NUM>
  where <NUM> is the length of the suffix for tree pruning.

EXAMPLE:

  make Hanoi14x4.stateCount N=1

RUN:

  ./<name_of_the_problem>.stateCount <depth>

RUN EXAMPLE:

  ./Hanoi14x4.stateCount 12
